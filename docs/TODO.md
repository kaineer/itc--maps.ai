## Подстройка моделей под требования размещения

 * Сначала нужно сделать так, чтобы модели можно было выбирать
 * По возможности, хотелось бы, если мы выбрали модель, выбирать сразу модель И полигоны, которые с ней связаны
 * После этого, если получится, хотелось бы заново запускать режим выравнивания и подправлять
 * Нужно исправить изменение масштаба (почему-то оно вышло слишком грубым)


### Что делаем

 * Выносим keyboard modifiers в обработчики (чтоб можно было использовать)
   * keyboardModifiers.ts
   * ModelBuilding.tsx
 * Получаем значения из Building и показываем в форме, в которой редактируем адрес и высоту
